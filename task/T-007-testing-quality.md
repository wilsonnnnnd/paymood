# T-007 — Testing & quality

Goal
- Add test runner and linters, and ensure computation functions have unit coverage.

Background
- Use lightweight tools like Vitest or Jest and ESLint + Prettier.

Scope
- Add config files and npm scripts for `test`, `lint`, `format`.

Requirements
- Tests for `lib/earnings.ts` covering main scenarios.

Acceptance Criteria
- `npm run test` runs and the core calculations pass locally.

Test Commands
- `npm run test`

Definition of Done
- Test and lint scripts present and used in verification steps.
