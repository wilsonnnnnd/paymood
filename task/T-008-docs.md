# T-008 — Documentation & README

Goal
- Update `README.md` with run, build, test, and design assumptions.

Background
- Provide clear developer onboarding and verification steps.

Scope
- Create/overwrite `README.md` with commands, architecture notes, and testing instructions.

Requirements
- Include commands for `npm install`, `npm run dev`, `npm run test`, and `npm run build`.

Acceptance Criteria
- Developer can follow README to run the app locally.

Test Commands
- None; manual verification.

Definition of Done
- README updated and linked from `task/task.md` registry.
