# PR Template (prepared locally)

Summary

- Implement Workday Earnings Dashboard MVP scaffold, calculation library, hooks, components, tests, and docs.

Files changed

- `app/`, `components/`, `lib/earnings.ts`, `hooks/`, `test/earnings.test.ts`, `package.json`, `tsconfig.json`, Tailwind/PostCSS config, README.

Verification

- Run `npm install` then `npx next dev` and open the site.
- Run `npx vitest --run` to execute unit tests for the calculation library.

Notes

- User requested no PR creation; this file documents the PR content for manual creation if desired.
